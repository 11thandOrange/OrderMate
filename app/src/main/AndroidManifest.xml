<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.clover.sdk.permission.READ_ORDERS" />
    <uses-permission android:name="com.clover.sdk.permission.ORDERS_R" />
    <uses-permission android:name="com.clover.sdk.permission.MERCHANT_R"/>

    <uses-permission android:name="com.clover.sdk.permission.WRITE_ORDERS" />

    <application
        android:name="com.order.orderappclover.utils.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_order"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_order"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderAppClover"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>