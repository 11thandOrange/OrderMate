<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name="com.orderMate.utils.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderAppClover"
        tools:targetApi="31">
        <activity
            android:name="com.orderMate.activities.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.orderMate.activities.OverlayActivity"
            android:excludeFromRecents="true"
            android:taskAffinity="your.unique.task.affinity"
            android:theme="@style/AppTheme.Dialog" />

        <receiver
            android:name="com.orderMate.broadcast.LineItemAddedReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.clover.intent.action.LINE_ITEM_ADDED" />
                <action android:name="com.clover.intent.action.LINE_ITEM_DELETED" />
                <action android:name="clover.intent.action.V1_ORDER_BUILD_START" />
                <action android:name="clover.intent.action.V1_ORDER_BUILD_STOP" />
                <action android:name="com.clover.intent.action.ITEM_SELECT" />
                <action android:name="com.clover.intent.action.ORDER_SAVED" />
                <action android:name="com.clover.intent.action.ORDER_CREATED" />
                <action android:name=" clover.intent.action.ACTIVE_REGISTER_ORDER"/>
                <action android:name="com.clover.intent.action.PAYMENT_PROCESSED" />
            </intent-filter>
        </receiver>

        <service android:name="com.orderMate.fragment.FloatingWidgetService"/>
        
        <!-- Scheduled Order Notification Receiver (#83 requirement) -->
        <receiver
            android:name="com.orderMate.services.OrderNotificationReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>