<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/orderHistoryFragment">

    <fragment
        android:id="@+id/customFieldsFragment"
        android:name="com.order.orderappclover.fragment.customFields.CustomFieldsFragment"
        android:label="fragment_custom_fields"
        tools:layout="@layout/fragment_custom_fields">
        <action
            android:id="@+id/action_customFieldsFragment_to_orderHistoryFragment"
            app:destination="@id/orderHistoryFragment" />
    </fragment>
    <fragment
        android:id="@+id/orderHistoryFragment"
        android:name="com.order.orderappclover.fragment.orderHistory.OrderHistoryFragment"
        android:label="fragment_order_history"
        tools:layout="@layout/fragment_order_history">
        <action
            android:id="@+id/action_orderHistoryFragment_to_customFieldsFragment"
            app:destination="@id/customFieldsFragment" />
        <action
            android:id="@+id/action_orderHistoryFragment_to_orderDetailFragment"
            app:destination="@id/orderDetailFragment" />
    </fragment>
    <fragment
        android:id="@+id/orderDetailFragment"
        android:name="com.order.orderappclover.fragment.orderDetail.OrderDetailFragment"
        android:label="fragment_order_detail"
        tools:layout="@layout/fragment_order_detail">
        <action
            android:id="@+id/action_orderDetailFragment_to_orderHistoryFragment"
            app:destination="@id/orderHistoryFragment" />
        <argument
            android:name="order"
            app:argType="string" />
        <argument
            android:name="employeeName"
            app:argType="string" />
        <argument
            android:name="orderStatus"
            app:argType="string" />
        <argument
            android:name="created"
            app:argType="long" />
        <argument
            android:name="total"
            app:argType="integer" />
        <argument
            android:name="curr"
            app:argType="string" />
        <argument
            android:name="tax"
            app:argType="integer" />
        <argument
            android:name="amount"
            app:argType="integer" />

    </fragment>

</navigation>